# Capture scratch space. The spell art the site draws is converted straight out
# of the game files into icons/ by tools/icons.py, so nothing here is shipped.
screenshots/
.playwright-mcp/

# Local dev files
start.bat
capture_tool.py
*.pyc
__pycache__/
.claude/

# Base-game icon source (9 MB); only the converted PNGs in icons/ are needed
war3.w3mod/

# File list tools/casc.py caches after its first read of the game install
tools/casc_files.pkl
